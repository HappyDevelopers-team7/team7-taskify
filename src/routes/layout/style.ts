import styled from 'styled-components';

const StWrapper = styled.div`
  height: 100vh;
  padding-top: 70px;

  #container {
    padding-left: 300px;
    width: 100%;
    height: 100%;
  }
`;

export default StWrapper;
