import styled from 'styled-components';

const StWrapper = styled.div`
  height: 100vh;

  #container {
    padding-top: 70px;
    padding-left: 300px;
    width: 100%;
  }
`;

export default StWrapper;
