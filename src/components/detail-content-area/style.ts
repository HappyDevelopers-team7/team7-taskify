import styled from 'styled-components';

const StDetailArea = styled.div`
  .text-box {
    color: #000;
    font-size: 1.4rem;
    font-weight: 400;
    line-height: 24px;
    margin: 16px 0;
  }

  .image-box {
    width: 100%;
    height: 100%;

    img {
      width: 100%;
      height: 100%;
      object-fit: contain;
    }
  }
`;

export default StDetailArea;
